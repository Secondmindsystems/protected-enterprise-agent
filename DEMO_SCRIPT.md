# 10-15 Minute Technical Demo Script

## 0:00-1:30 - Thesis and trust map

Show `ARCHITECTURE.md`. State the tested claim: the prototype preserves business utility while denying enumerated raw synthetic identifiers to tested model-facing and application-controlled surfaces.

## 1:30-3:00 - Vendor reality

Show the pinned upstream SHA and component versions. Start the two official local stacks. Run direct documented API calls and show the actual, unscripted response shapes.

## 3:00-5:00 - Sensitive source to protected index

Show one fictional source fixture in the trusted raw zone. Run ingestion. Show Data Discovery entity classes, then show the protected stored record and non-sensitive metadata. Do not display the leak manifest contents on screen.

## 5:00-7:00 - Legitimate support task

Run scenario S1. Show that protected retrieval preserves the service tier, state, case, and policy action. Show the exact dispatch assertion and useful response. Open the utility result.

## 7:00-9:00 - Exfiltration and prompt injection

Run S2 and S3. Show the observed Semantic Guardrails batch outcome, score, and labels and the normalized application decision. Confirm that blocked requests never retrieve context or call the model.

## 9:00-10:30 - Direct PII and fail-closed behavior

Run S4 with fictional direct user PII and show the pinned Semantic Guardrails outcome. In the qualified run this request is blocked before retrieval; no claim of input rehydration or downstream transformation is made. Then use the automated fail-closed tests to show provider failure, malformed results, and canary detection produce explicit failure with zero raw forwarding and sanitized evidence.

## 10:30-12:00 - Independent proof plane

Run the canonical tests and leak scanner. Open the machine-readable security, adversarial, utility, fail-closed, provenance, and evidence-integrity results. Run `render_execution_proof.py` and show `PROVEN`. Emphasize that the renderer only reads receipts and that the scanner searches exact canaries rather than trusting application narration.

Run `demo_fault_injection.py`. Show that a deterministic required-guardrail outage prevents model dispatch, a temporary missing vendor receipt renders `NOT PROVEN`, and restoring the guardrail restores the identical legitimate workflow. State explicitly that this is deterministic application fault injection, not evidence of vendor service behavior; the live-vendor evidence was established separately.

## 12:00-13:30 - Reproducibility and limits

Show clean-clone setup, the frozen commit, and `KNOWN_LIMITATIONS.md`. State clearly that this is a prototype; application surrogates are not Protegrity tokenization; and tested absence is scoped to enumerated fixtures and surfaces.

Close with: "Protegrity supplies the controls. Protected Enterprise Agent proves they governed the execution. Don't trust the README. Run the adjudicator."
